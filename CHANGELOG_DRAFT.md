# NextLNMP v2.0 变更草稿

> 状态：**待发版**。真机测试矩阵尚未跑完（见文末「验证状态」），
> 未跑完的部分不得写成"已验证"。

## 一、AI 自愈生态（本版主线）

安装失败时不再只留一句"请把日志发到 QQ 群"，而是当场进入对话：AI 读日志、
要证据、给修复，用户逐条决定是否执行。若判定为镜像缺件，网关侧还会把供应链
补齐，让同一个坑对后来者消失。

- **客户端 `include/ai-assist.sh`**：失败自动进入对话，无需用户主动求助。
  只读诊断命令走白名单（用户看得见、可拒绝），修复命令走危险模式黑名单。
- **网关**：DeepSeek v4-flash + 两级缓存（L1 错误签名归一化零 token、L2 前缀缓存）。
- **自愈三闸**：上游域名白名单（只从官方源拉）、文件名/目录模式匹配、
  清单条目须经公开提交。
- **关闭方式**：`NEXTLNMP_AI=n`。

## 二、CLI 重构（消除漂移债）

三个 CLI（lnmp / lnmpa / lamp）此前是三份近似复制，改一处忘两处是常态。

- 改为构建期组装：公共函数抽出 38 个，单一事实源。
- 多版本 PHP 选择、数据库/PHP 菜单改为表驱动，菜单↔版本表↔安装函数三方对齐。
- **六道 CI 闸**（CLI Guard）防止重构回归：漂移、指纹等价、渲染保真、
  一致性断言、php-select 单测、`bash -n`。本版又加了第 7、8 道
  （ai-assist 安全闸门、依赖安装容错外壳）。

## 三、真机测试暴露并修掉的缺陷

- **收尾自检把"装了"当成"跑起来了"**（`4fe5152`）——只判文件存在，
  于是数据库启动失败 4 次仍打印完整成功横幅，AI 救援也因此从不触发。
  改为文件在但进程不活即明确报 `installed but not running`，
  nginx / 数据库 / PHP-FPM / Apache 全部同款处理。
- **预编译包与 CPU 指令集不兼容时卡到超时**（`18078fd`）——Rocky 9 真机上
  MariaDB 12.3.2 的客户端工具在无 AVX 的 CPU 上 SIGILL，而 init 脚本的就绪探测
  正是 `mysqladmin ping`，用户只看见"启动失败"。现在解包后预检服务端二进制，
  启动后按信号判客户端（128+N，SIGILL=4→132），命中即给出可执行的出路。
- **恢复出厂删不掉网站目录**（`aa12526`）——安装给 `.user.ini` 加了 `chattr +i`，
  `rm -rf` 静默失败、目录残留，脚本却宣称"已恢复至初始状态"。
- **CLI 加数据库时空名死循环**（`ef1691c`）——自动名为空且读到 EOF 时无限刷屏。
- **依赖清单里的上古包名刷一屏 Error**（`9515a80`、`ffe32cc`）——EL9 / Debian 12+
  里已改名或消失的可选包（libpng10-devel、krb5、libidn、pspell-devel、
  libc-client-devel、zlibc、libpng12-dev 等）装不上会吐 `Error:`，
  用户以为装挂了，AI 救援也会被带偏。现在只吞"这个发行版没这个包"，
  源坏 / 网断 / 依赖冲突照样原样报出并返回非 0。
- **三处会往日志丢假错误的写法**（`b1ace7a`）——行为不变的去噪。

## 四、供应链

- 镜像补货至 126/126，覆盖体检全绿。
- 魔搭（ModelScope）副镜像，三级容灾：主镜像 → 副镜像 → 官方上游，
  任一级失败自动降级并逐包 SHA256 校验。
- CI 每日自动发版（需 CI 全绿方可放行）。

## 验证状态

| 轮次 | 系统 | 状态 |
|------|------|------|
| 1–4 | Ubuntu 22.04 | ✅ 含 OOM→AI 救援杀手级验收 |
| 5 | Rocky 9.7 | ✅ 抓到两个真缺陷并修复 |
| 5b | Rocky 9.7 + MariaDB 10.11 | 🔄 进行中 |
| 6 | CentOS 7 | ⏳ 未跑 |
| 7 | Debian 12（LAMP） | ⏳ 未跑 |
| 8 | Debian 13（PHP 8.4） | ⏳ 未跑 |

发版前还需：矩阵跑完、`HEAL_ENABLE=1` 下的自愈全链路验收、v2 合入 main。
